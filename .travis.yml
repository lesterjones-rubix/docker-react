sudo: required
services:
  - docker

before_install:
  - docker build -t lesterjones/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lesterjones/docker-react npm run test

language: generic
